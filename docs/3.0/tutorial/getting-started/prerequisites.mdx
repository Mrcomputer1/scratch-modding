---
sidebar_position: 1
---
import DocVersionSwitcher from '@site/src/components/DocVersionSwitcher';

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

# Prerequisites

<DocVersionSwitcher versions={{
    agpl: {
        name: "AGPL",
        path: "../../../3.0/tutorial/getting-started/prerequisites",
    },
    bsd: {
        name: "BSD",
        path: "../../../3.0-bsd/tutorial/getting-started/prerequisites",
    },
}} active="agpl" />

:::info
This tutorial uses the newer `scratch-editor`-based version of Scratch, which uses the [GNU Affero General Public License v3.0](https://github.com/scratchfoundation/scratch-editor/blob/810ac12eaa1a1eb19d05fcb37f486ad1c4c56314/LICENSE) license.

If you want to use an older version of Scratch 3.0, that is still under the [BSD 3-Clause](https://github.com/scratchfoundation/scratch-gui/blob/1c9ff0dcfcfc9ffee992158bf906e590ad2fb566/LICENSE) license, see [this](../../../3.0-bsd/tutorial/getting-started/prerequisites.mdx) version of the tutorial.

The newer AGPL-licensed version of Scratch requires you to release the source code of your mod if you publish your mod. The older BSD-licensed version of Scratch doesn't require this.
:::

## Software and Hardware Requirements

### A Computer
You will need a computer (Windows, macOS and Linux all work) to mod Scratch 3.0. You will also need to be able to install software on it. If you do not have a computer or are unable to install software on the computer, you might be able to use an online service like GitHub Codespaces or Replit.

### A Web Browser
As Scratch 3.0 runs in a web browser, you will need a web browser. Since you are reading this, you probably already have one. Any browser that works with Scratch 3.0 will be fine.

### A Code Editor
You will need a code editor to write code for your Scratch mod in. You could technically use any text editor, but you should use an editor designed for code, as they generally provide useful features like syntax highlighting, suggestions and other useful code-specific features.

Here are some code editors:
* [Visual Studio Code](https://code.visualstudio.com) <span class="badge badge--primary">Recommended</span>
* [Zed](https://zed.dev/)
* _please suggest some more_

### Node.js
Node.js is used by Scratch's development tools. You can download Node.js [here](https://nodejs.org/en).

### A Node.js Package Manager (NPM, Yarn, etc.)
You need a Node.js package manager to install Scratch's depenedncies. Node.js comes with NPM, but you could also use [Yarn](https://yarnpkg.com/).

### Git <span class="badge badge--primary">Recommended, not required</span>
[Git](https://git-scm.com) is a version control system. Using Git well will allow you to view older versions of your mod and, with GitHub, allow you to collaborate with others to make a mod. Using Git also allows you to more easily release your mod using GitHub and GitHub Pages.

## Useful Knowledge

### JavaScript <span class="badge badge--primary">Recommended</span>
JavaScript is the main language used by Scratch 3.0. Knowing at least the basics of JavaScript will be very helpful in modding Scratch 3.0.

### TypeScript <span class="badge badge--primary">Recommended</span>
Some parts of Scratch have been ported to TypeScript. Knowing at least the basics of TypeScript will also be very helpful in modding Scratch 3.0. TypeScript is based on JavaScript.

### Command Line <span class="badge badge--primary">Recommended</span>
You need to use some commands to setup, build and run Scratch 3.0. You should at least know how to launch the command line on your computer or in your code editor and how to navigate your computer with it (`cd`, `dir`/`ls`, etc.).

* **Windows:** Windows Terminal, Command Prompt or Windows PowerShell
* **macOS:** Terminal
* **Linux:** The exact name may depend on Linux distribution. Ubuntu calls it Terminal.

### Git <span class="badge badge--primary">Recommended</span>
If you want to use Git and GitHub, you will need to know at least how to clone a repository and commit. If you want to use GitHub, you will also need to know how to fork a repository on GitHub and push to the repository.

### `require`, `module.exports` (CommonJS) and `import`, `export` (ES Modules)
Scratch 3.0 is made up of many files and packages and needs a way to use resources from another file and a way to make resources available to other files. Some files use the older CommonJS method for importing resources (the `require` function) and exporting resources (the `module.exports` object). Other files use the newer ES Modules method for import resources (the `import` statement) and exporting resources (the `export` statement).

### HTML
While HTML is not really used much by Scratch, the concepts can be useful for JSX, which is needed to modify Scratch's interface.

### CSS
Knowledge of CSS is useful for modifying the appearance of the Scratch interface.

### React and JSX
`scratch-gui` (the user interface for Scratch 3.0) uses React for the user interface. If you want to modify the interface, it will be useful to know it. Scratch also uses JSX, which is just JavaScript with the ability to write a strict form of HTML directly in the JavaScript.